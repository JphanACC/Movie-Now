<!DOCTYPE html>
<html lang="en">


<%- include ('../partials/head') %>

    <body>
        <%- include ('../partials/head-nav') %>
            <div class="all-theatres-list-div">
                <p>All Theatres</p>
                <% theatres.forEach((theatre, index) => { %>
                    <%- include ('../partials/theatreTemplate', {oneTheatre: theatre}) %>
                        <% }) %>
            </div>
    </body>

</html>